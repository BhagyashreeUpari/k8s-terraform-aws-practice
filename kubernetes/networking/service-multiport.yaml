apiVersion: v1
kind: Service
metadata:
  name: web-app-multiport
  namespace: default
spec:
  selector:
    app: web-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  - name: metrics
    protocol: TCP
    port: 8080
    targetPort: 8080
    