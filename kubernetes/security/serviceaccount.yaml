# ServiceAccount for payment service
# Pods use this as their identity when calling K8s API

apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-service-sa
  namespace: default
  labels:
    app: payment-service